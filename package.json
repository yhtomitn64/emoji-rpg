{
  "name": "rpg",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.js",
    "relock": "rm -rf node_modules package-lock.json && npm install --registry https://registry.npmjs.org/"
  },
  "devDependencies": {
    "jsdom": "^30.0.1"
  }
}
